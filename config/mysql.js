const config = {
  	host: 'localhost',  		//IP本地
	user: 'root',			//用户
	password: '123456',		//密码
	database: 'taotao', 	//数据库
	port: 3306, 			//端口
	multipleStatements:true //是否开启多条sql语句查询，因项目需要开启，默认为false~
}

module.exports = config;
